import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Phantom Forecast - AI Market Intelligence",
  description:
    "Pluralistic strategic intelligence through competing investor phantom personas. See markets through the eyes of Buffett, Munger, Burry, Dalio, Ackman, and Lynch.",
  keywords: [
    "AI investing",
    "market analysis",
    "Warren Buffett",
    "investment strategy",
  ],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className="dark">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
